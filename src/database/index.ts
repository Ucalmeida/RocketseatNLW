import { createConnection } from 'typeorm';

// Chamar o createConnection
createConnection(); // Isso vai fazer com que o banco seja inicializado. 
                    // Com que ele crie uma conexão com o banco de dados.